[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Show short test summary for all failures and errors
addopts = -v --tb=short -q

# Markers (for future use)
markers =
    unit: Pure-logic tests, no database required
    regression: Integration tests against local Docker SQL Server
